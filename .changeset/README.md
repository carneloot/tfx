# Changesets

Changesets describe releases for public workspace packages. Private `carneloot-bot` is excluded from versioning and publication.

Slice 1 validates package contents only with `pnpm pack` dry runs. It does not publish packages.
